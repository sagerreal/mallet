export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "3rem", maxWidth: 720 }}>
      <h1 style={{ fontSize: "2rem", marginBottom: ".5rem" }}>Mallet</h1>
      <p style={{ color: "#555" }}>
        Production app scaffold (Phase 0). The product UI is ported from the prototype
        in later phases. See <code>docs/superpowers/plans</code> for the build plan.
      </p>
    </main>
  );
}
