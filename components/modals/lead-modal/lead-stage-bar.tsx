/**
 * components/modals/lead-modal/lead-stage-bar.tsx
 * REMOVED — the prototype has NO "Move stage" bar in the lead modal.
 * Stage is shown as a pill in the header only (LeadHeader).
 * Footer (Clean up / Delete) is now in MoreDetails.
 * This file is kept as an empty export to avoid breaking any stale imports.
 */

export {};
